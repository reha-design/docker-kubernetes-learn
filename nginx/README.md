# nginx 실습 설정 파일

리버스 프록시/nginx 보충 학습([notes/nginx-reverse-proxy.md](../notes/nginx-reverse-proxy.md))에서
직접 작성하고 테스트한 `nginx.conf` 예제들을 모아두는 폴더. `manifests/dayNN/`이 K8s YAML을
모아두는 것과 같은 역할을, 여기서는 순수 nginx 설정 파일에 대해 한다.
